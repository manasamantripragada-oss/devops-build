version: '3.8'

services:
  app:
    image: manasadevi09/devops-build
    container_name: devops-build-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
